<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="#">Blog symfony</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor03">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path("public_site_home") }}">Home
					<span class="sr-only">(current)</span>
				</a>
			</li>
		</ul>
		{% if app.user %}
			{% if is_granted('ROLE_ADMIN') %}
				<a href="{{ path("posts_index") }}" class="btn btn-secondary my-2 my-sm-0">Posts</a>
				<a href="{{ path("tags_index") }}" class="btn btn-secondary my-2 my-sm-0">Tags</a>
			{% endif %}
			<a href="{{ path("logout") }}" class="btn btn-secondary my-2 my-sm-0">Logout</a>
		{% else %}
			<a href="{{ path("app_login") }}" class="btn btn-secondary my-2 my-sm-0">Login</a>
		{% endif %}
	</div>
</nav>
